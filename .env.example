# {{project name}} — local environment seed.
# Every name here is declared in docs/VARIABLES.md first; a variable used in
# code but not declared there does not exist. Secret VALUES never live here.

PROJECT_SLUG={{project slug}}
SECRET_KEY={{dev secret seed}}
DEBUG=true
