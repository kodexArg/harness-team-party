# Local environment. Copy to .env and fill values.
# .env is gitignored. Names are declared in docs/VARIABLES.md.
# Secret values never belong in this file.

# OpenRouter Chat Completions. Same account as the local ALVS tools.
OPENROUTER_API_KEY=sk-or-v1-REPLACE_ME
# Model slug (org/model).
OPENROUTER_MODEL_ID=z-ai/glm-5.3-flash
