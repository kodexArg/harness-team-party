# CHANGELOG

## v0.1.0

Clean baseline for the code in this repository. Skills, the Graphify graph, session hooks, and tests stay. Governance scripts, unused role stubs, report skills, and unfilled template slots are gone. Local model access is `OPENROUTER_API_KEY` in `.env` (gitignored), declared in `docs/VARIABLES.md`.
