# docs/contracts/

Payload shapes for [`docs/INTERFACES.md`](../INTERFACES.md) rows that are too large to inline. One document per cluster, named `contract-<cluster>.md`.

This directory is empty until a row needs a separate shape.
